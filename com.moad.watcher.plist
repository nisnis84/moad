<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.moad.watcher</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>__MOAD_DIR__/watch-refresh.sh</string>
    </array>

    <!-- Fires when the Downloads directory itself changes. Top level only, which
         matches the hub's max_depth of 1. -->
    <key>WatchPaths</key>
    <array>
__WATCH_PATHS__
    </array>

    <!-- Also sync once at login, so any event missed while logged out self-heals. -->
    <key>RunAtLoad</key>
    <true/>

    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
    </dict>

    <key>StandardOutPath</key>
    <string>__MOAD_DIR__/watch-launchd.log</string>
    <key>StandardErrorPath</key>
    <string>__MOAD_DIR__/watch-launchd.log</string>
</dict>
</plist>
